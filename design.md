# Featable 디자인 시스템

## 방향

국내 플랫폼 톤(토스, 당근, 무신사 구조)을 기준으로 한다. 에디토리얼 무드보다 **면으로 구분되는 플랫폼 UI**가 우선이다.

- 페이지 배경은 연회색 면(`--surface: #f7f7f8`), 카드는 흰 면. 보더로 카드를 그리지 않는다.
- 그림자는 hover 시에만. 카드 lift(떠오르는 모션), 이미지 zoom, 배너 zoom 금지. 3D 틸트 프로필 카드만 예외.
- 커머스(쿠팡 등) 레퍼런스는 배제. 가격 노출, 판매 문구를 발견 플로우에 넣지 않는다.

## 토큰

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--surface` | `#f7f7f8` | 페이지 배경, 검색박스, 칩 |
| `#fff` | 카드, 헤더 | 면 구분의 기본 |
| `--muted` | 회색 텍스트 | 카드 설명, 메타 |
| `--accent` | 오렌지 | CTA, 강조 배지 |

## 타이포

- 자간은 헤드라인 `-.02em` ~ `-.03em`, 본문 `-.01em` 기본.
- 라벨(eyebrow)은 소문자 강조 대신 조용한 회색 텍스트. 굵은 대문자 라벨 금지.
- 카드 제목 16-17px / weight 650, 설명 13px / muted. 메타 텍스트 12px.

## 카드

### 이미지형 (프로덕트, 스토리, 행사)

- 흰 면 없이 이미지 + 텍스트 블록. radius 10-12px.
- hover는 그림자(`0 6px 20px rgba(17,17,17,.07)`)만. 이미지 확대 금지.
- 메타는 배지 + 브랜드명. 조회수 등 숫자 메타는 목록 카드에서 제외.

### 로우형 (브랜드, 커뮤니티)

- 흰 면 + radius 12px + padding 18px 20px. 로고 56px / radius 12px / `#f4f4f5` 배경.
- 구조: 로고 → 이름+배지(한 줄) → 설명(한 줄 말줄임) → 화살표(세로 중앙).
- 배지는 제목 위 줄이 아니라 제목 옆에 붙인다.

## 모션

- transition은 `box-shadow`, `color`, `background` 위주로 0.15-0.2s.
- transform 기반 hover 모션(translateY, scale)은 추가하지 않는다.

## 참고

- 채택: 토스의 면 기반 카드와 절제된 모션, 당근의 로우형 목록, 무신사의 그리드 밀도.
- 배제: 커머스형 가격 노출, 굵은 대문자 라벨, 다중 hover 모션.
