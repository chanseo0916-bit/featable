# Featable 디자인 가이드

> 방향: 에디토리얼이 아니라 **플랫폼**. 토스·국내 플랫폼 서비스처럼 정보가 명확하고, 읽기 편하고, 장식 없이 정돈된 UI.

## 핵심 원칙

1. **가독성 최우선** — 본문 15-16px, 보조 텍스트 13px 이상. 12px 미만 텍스트는 로고 워터마크 같은 특수한 경우 외 금지.
2. **노이즈 제거** — 섹션마다 붙는 영어 eyebrow 라벨(JUST IN, BUILDERS OF THE WEEK 등)은 기본 제거. 제목만으로 충분히 의미가 전달되면 라벨을 넣지 않는다.
3. **레이아웃 유지** — 구조(그리드/섹션 순서/컴포넌트 배치)는 건드리지 않고, 색·폰트·간격·테두리만 다듬는다.
4. **플랫폼 신뢰감** — 카드는 헤어라인 테두리 + 여백으로 구분. 무거운 그림자나 그라디언트 남발 금지.
5. **오렌지는 신호로만** — CTA, 활성 상태, D-day, 링크 호버에만 사용. 배경 대량 칠하거나 여러 요소에 동시 사용 금지.

## 디자인 토큰

```css
--background: #FFFFFF;
--foreground: #111111;
--muted: #6B7280;
--border: #E5E7EB;
--accent: #EF4125;
--accent-hover: #D9361E;
--accent-soft: #FFF0ED;
```

### 타이포그래피

| 용도 | 크기 | 굵기 | 비고 |
|---|---|---|---|
| 페이지 H1 | clamp(32px, 5vw, 48px) | 700 | letter-spacing -0.02em 이내 |
| 섹션 H2 | 22-26px | 650-700 | |
| 카드 제목 | 16-18px | 600-650 | |
| 본문 | 15-16px | 400-450 | |
| 보조 텍스트 (브랜드명, 메타) | 13px | 450-500 | |
| 캡션·배지 | 12px 최소 | 600+ | 8-11px 전면 금지 |

- 폰트: Pretendard 유지.
- letter-spacing: 한국어 텍스트에는 -0.05em 이하 과도한 조임 금지. -0.01 ~ -0.03em 권장.

### 간격

- 카드 내부 패딩: 20-24px.
- 카드 간 gap: 16-20px.
- 섹션 상하 여백: 64-88px (모바일 48-64px).
- 관련 있는 요소는 가깝게, 다른 그룹은 멀리 (근접성 원칙).

### 컴포넌트 규칙

**카드**

- 흰 배경 + 1px var(--border) + border-radius 10-12px.
- 그림자는 호버 시에만 은은하게 (`0 8px 24px rgba(17,17,17,.06)` 수준).
- 호버 시 카드를 위로 띄우거나 이미지를 확대하지 않는다. 테두리·색·그림자 변화로만 클릭 가능성을 알린다.
- 예외: 직접 조작하는 프로필 카드의 3D 틸트는 핵심 인터랙션으로 유지한다.
- 카드 안 요소: 이미지 > 제목 > 한 줄 설명 > 메타(파운더/조회수) 순. 계층 3단계 초과 금지.

**버튼**

- Primary: 오렌지 배경 + 흰 텍스트. radius 8-10px.
- Secondary: 흰 배경 + 1px 테두리.
- 버튼 안 아이콘+텍스트 조합 시 gap 6-8px.

**배지**

- 한 카드에 배지 최대 1-2개. NEW/D-day/카테고리 중 우선순위 높은 것만.
- accent-soft 배경 + accent 텍스트, 또는 연회색 배경 + 진회색 텍스트.

**폼**

- 인풋 높이 44-52px, font-size 15-16px (모바일 iOS 줌 방지).
- placeholder는 muted 색상, 입력 텍스트는 foreground.

## 적용 순서

1. globals.css에서 8-11px font-size를 12px 이상으로 상향 (단계적: 먼저 13px 기준 적용 후 시각 확인).
2. 홈 화면 eyebrow 라벨 제거 또는 축소 - 제목과 중복되는 것부터.
3. 알림 패널·푸터 등 보조 UI의 작은 폰트 정리.
4. 빌드 + localhost:3000에서 실제 화면 확인.

## 참고

- 토스 디자인 원칙: 명확함, 일관성, 여백으로 위계 만들기.
- Product Hunt / 와디즈 / 이벤터스: 카드형 정보 구조, 필터와 리스트의 명확한 위계.
- 현재 코드베이스 문제: font-size 8-11px 사용이 약 638곳, eyebrow 라벨 51곳. 이 두 가지가 "작아 보이고 복잡해 보이는" 주요 원인.
