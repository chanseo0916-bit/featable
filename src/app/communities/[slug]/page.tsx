import { notFound } from "next/navigation";
import { Footer, Header, Badge } from "@/components/site-shell";
import { communities, founders, partners } from "@/lib/mock";
export default async function CommunityDetailPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const community = communities.find((item) => item.slug === slug); if (!community) notFound(); const people = (community.founderSlugs ?? []).map((item) => founders.find((f) => f.slug === item)).filter(Boolean); return <><Header /><main className="shell detail-page community-detail"><div className="community-identity"><img className="community-logo large" src={community.logoUrl} alt="" /><div><Badge tone="orange">{community.field}</Badge><h1>{community.name}</h1><p>{community.intro}</p></div></div><div className="community-about"><p className="eyebrow">ABOUT COMMUNITY</p><p>{community.intro}. 함께하는 창업가와 브랜드의 이야기를 Featable에서 만나보세요.</p></div><section><h2>함께하는 창업가</h2><div className="people-list">{people.map((founder) => founder && <div className="founder-callout" key={founder.slug}><span className="avatar"><img src={founder.avatarUrl} alt="" /></span><div><h3>{founder.name}</h3><p>{founder.headline}</p></div></div>)}</div></section></main><Footer partners={partners} /></>; }
