import { notFound } from "next/navigation";
import { Footer, Header, Badge } from "@/components/site-shell";
import { partners } from "@/lib/mock";
import { getSupportPrograms } from "@/lib/data";
export default async function SupportDetailPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const supportPrograms = await getSupportPrograms(); const program = supportPrograms.find((item) => item.slug === slug); if (!program) notFound(); return <><Header /><main className="detail-page"><div className="detail-kicker"><Badge tone="orange">{program.status}</Badge><span>마감일 {program.closeAt}</span></div><h1>{program.name}</h1><p className="detail-lede">{program.agency}에서 진행하는 창업 지원 프로그램입니다.</p><div className="support-detail-grid"><div><p className="eyebrow">지원 대상</p><h2>{program.target}</h2></div><div><p className="eyebrow">지원 규모</p><h2>{program.amount ?? "사업별 상이"}</h2></div><div><p className="eyebrow">지원 지역</p><h2>{program.region}</h2></div><div><p className="eyebrow">주요 내용</p><h2>{program.benefits}</h2></div></div><a className="button" href={program.applyUrl}>공식 공고 확인하기 ↗</a></main><Footer partners={partners} /></>; }
