import Link from "next/link";
import { Footer, Header, Badge } from "@/components/site-shell";
import { partners, supportPrograms } from "@/lib/mock";
const dday = (date: string) => Math.max(0, Math.ceil((new Date(date).getTime() - Date.now()) / 86400000));
export default function SupportPage() { return <><Header /><main className="shell listing-page"><div className="listing-heading"><div><p className="eyebrow">STARTUP SUPPORT</p><h1>지원사업</h1><p>내 브랜드에 맞는 창업 지원 정보를 찾아보세요.</p></div></div><div className="filter-chips support-filters"><a className="active" href="/support">전체</a><a href="/support?region=전국">전국</a><a href="/support?region=서울">서울</a><a href="/support?field=기술">기술</a></div><div className="support-list">{supportPrograms.map((program) => <Link href={`/support/${program.slug}`} className="support-row" key={program.slug}><div className="support-dday"><strong>D-{dday(program.closeAt)}</strong><span>{program.status}</span></div><div className="support-main"><h3>{program.name}</h3><p>{program.agency} · {program.region} · {program.target}</p></div><strong className="support-amount">{program.amount}</strong><Badge tone={program.status === "마감임박" ? "orange" : "default"}>{program.status}</Badge><span className="arrow">→</span></Link>)}</div></main><Footer partners={partners} /></>; }
